<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nos Services - YuniGate</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
   
    <style>
        .hover-lift {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .hover-lift:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
        .card {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
        }
        .dark .card {
            background: #1f2937;
        }
        .btn {
            display: inline-flex;
            align-items: center;
            padding: 0.75rem 2rem;
            border-radius: 0.5rem;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.2s;
        }
        .btn-secondary {
            background-color: #f1f5f9;
            color: #374151;
            border: 1px solid #e2e8f0;
        }
        .btn-secondary:hover {
            background-color: #e2e8f0;
            transform: translateY(-1px);
        }
        .dark .btn-secondary {
            background-color: #374151;
            color: #f8fafc;
            border-color: #4b5563;
        }
        .dark .btn-secondary:hover {
            background-color: #4b5563;
        }
    </style>
</head>
<body class="bg-gray-50">
   
    <section class="py-16 bg-white dark:bg-secondary-900">
        <div class="max-w-6xl mx-auto px-4">
            <!-- Section Title -->
            <div class="text-center mb-12">
                <h2 class="text-4xl font-bold text-secondary-900 dark:text-white mb-4">
                    Nos <span class="text-primary-500">services</span>
                </h2>
                <p class="text-lg text-secondary-700 dark:text-secondary-300 max-w-3xl mx-auto">
                    YuniGate vous accompagne à chaque étape de votre projet d'études au Maroc, de la préinscription jusqu'à votre intégration complète.
                </p>
            </div>
            
            <!-- Services Grid -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
  <!-- 1. Orientation et inscription -->
  <div class="card hover-lift p-6" 
       x-data="{ hover: false }"
       x-on:mouseenter="hover = true" 
       x-on:mouseleave="hover = false">
    <div 
        class="w-14 h-14 flex items-center justify-center rounded-full mb-5 text-primary-500 transition-all duration-300"
        x-bind:class="hover ? 'bg-primary-500 text-white' : 'bg-primary-100 dark:bg-primary-900/30'">
        <span class="font-bold text-lg">1</span>
    </div>
    <h3 class="text-xl font-bold mb-3 text-secondary-900 dark:text-white">Orientation et inscription</h3>
    <p class="text-secondary-700 dark:text-secondary-300">
        Orientation personnalisée et inscription dans les universités privées marocaines partenaires selon votre profil et vos aspirations.
    </p>
  </div>

  <!-- 2. Recherche de logement -->
  <div class="card hover-lift p-6" 
       x-data="{ hover: false }"
       x-on:mouseenter="hover = true" 
       x-on:mouseleave="hover = false">
    <div 
        class="w-14 h-14 flex items-center justify-center rounded-full mb-5 text-primary-500 transition-all duration-300"
        x-bind:class="hover ? 'bg-primary-500 text-white' : 'bg-primary-100 dark:bg-primary-900/30'">
        <span class="font-bold text-lg">2</span>
    </div>
    <h3 class="text-xl font-bold mb-3 text-secondary-900 dark:text-white">Recherche de logement</h3>
    <p class="text-secondary-700 dark:text-secondary-300">
        Accompagnement dans la recherche d’un logement adapté à votre budget et vos préférences.
    </p>
  </div>

  <!-- 3. Prise de billet -->
  <div class="card hover-lift p-6" 
       x-data="{ hover: false }"
       x-on:mouseenter="hover = true" 
       x-on:mouseleave="hover = false">
    <div 
        class="w-14 h-14 flex items-center justify-center rounded-full mb-5 text-primary-500 transition-all duration-300"
        x-bind:class="hover ? 'bg-primary-500 text-white' : 'bg-primary-100 dark:bg-primary-900/30'">
        <span class="font-bold text-lg">3</span>
    </div>
    <h3 class="text-xl font-bold mb-3 text-secondary-900 dark:text-white">Prise de billet</h3>
    <p class="text-secondary-700 dark:text-secondary-300">
        Assistance à la réservation des billets d'avion selon vos dates et conditions de voyage.
    </p>
  </div>

  <!-- 4. Accueil à l'aéroport -->
  <div class="card hover-lift p-6" 
       x-data="{ hover: false }"
       x-on:mouseenter="hover = true" 
       x-on:mouseleave="hover = false">
    <div 
        class="w-14 h-14 flex items-center justify-center rounded-full mb-5 text-primary-500 transition-all duration-300"
        x-bind:class="hover ? 'bg-primary-500 text-white' : 'bg-primary-100 dark:bg-primary-900/30'">
        <span class="font-bold text-lg">4</span>
    </div>
    <h3 class="text-xl font-bold mb-3 text-secondary-900 dark:text-white">Accueil à l'aéroport</h3>
    <p class="text-secondary-700 dark:text-secondary-300">
        Accueil chaleureux à votre arrivée au Maroc, avec transfert jusqu'à votre lieu de résidence.
    </p>
  </div>

  <!-- 5. Démarches administratives -->
  <div class="card hover-lift p-6" 
       x-data="{ hover: false }"
       x-on:mouseenter="hover = true" 
       x-on:mouseleave="hover = false">
    <div 
        class="w-14 h-14 flex items-center justify-center rounded-full mb-5 text-primary-500 transition-all duration-300"
        x-bind:class="hover ? 'bg-primary-500 text-white' : 'bg-primary-100 dark:bg-primary-900/30'">
        <span class="font-bold text-lg">5</span>
    </div>
    <h3 class="text-xl font-bold mb-3 text-secondary-900 dark:text-white">Démarches administratives</h3>
    <p class="text-secondary-700 dark:text-secondary-300">
        Aide à l’obtention de la carte de séjour, à l’ouverture de compte bancaire, à la souscription d’assurance, etc.
    </p>
  </div>
</div>


            
            <!-- CTA Button -->
            <div class="mt-12 text-center">
                <a href="/services" class="btn btn-secondary">
                    En savoir plus sur nos services
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M12.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                </a>
            </div>
        </div>
    </section>
</body>
</html>